on:
  push:
    branches: [ main ]

jobs:

  deploy:

    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Simular deploy
        run: echo "Deploy em produção realizado"
